/**
 * The enum SubtaskTypes contains all possible types of subtasks that can be used. It is saved in a subtask to easier
 * identify the type of a specific subtask.
 */

enum SubtaskTypes {
    Sql,
    MultipleChoice,
    PlainText,
    Instruction,
}

export default SubtaskTypes;
